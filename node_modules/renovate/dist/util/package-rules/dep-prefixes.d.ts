import type { PackageRule, PackageRuleInputConfig } from '../../config/types';
import { Matcher } from './base';
export declare class DepPrefixesMatcher extends Matcher {
    matches({ depName }: PackageRuleInputConfig, { matchDepPrefixes }: PackageRule): boolean | null;
    excludes({ depName }: PackageRuleInputConfig, { excludeDepPrefixes }: PackageRule): boolean | null;
}
