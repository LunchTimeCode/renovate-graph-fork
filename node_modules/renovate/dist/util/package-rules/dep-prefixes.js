"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DepPrefixesMatcher = void 0;
const tslib_1 = require("tslib");
const is_1 = tslib_1.__importDefault(require("@sindresorhus/is"));
const base_1 = require("./base");
class DepPrefixesMatcher extends base_1.Matcher {
    matches({ depName }, { matchDepPrefixes }) {
        if (is_1.default.undefined(matchDepPrefixes)) {
            return null;
        }
        if (is_1.default.undefined(depName)) {
            return false;
        }
        return matchDepPrefixes.some((prefix) => depName.startsWith(prefix));
    }
    excludes({ depName }, { excludeDepPrefixes }) {
        if (is_1.default.undefined(excludeDepPrefixes)) {
            return null;
        }
        if (is_1.default.undefined(depName)) {
            return false;
        }
        return excludeDepPrefixes.some((prefix) => depName.startsWith(prefix));
    }
}
exports.DepPrefixesMatcher = DepPrefixesMatcher;
//# sourceMappingURL=dep-prefixes.js.map