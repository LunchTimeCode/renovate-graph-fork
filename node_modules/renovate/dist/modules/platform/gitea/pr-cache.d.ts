import type { GiteaHttp } from '../../../util/http/gitea';
import type { Pr } from '../types';
export declare class GiteaPrCache {
    private repo;
    private author;
    private cache;
    private constructor();
    static forceSync(): void;
    private static init;
    private getPrs;
    static getPrs(http: GiteaHttp, repo: string, author: string): Promise<Pr[]>;
    private addPr;
    static addPr(http: GiteaHttp, repo: string, author: string, item: Pr): Promise<void>;
    private reconcile;
    private sync;
}
